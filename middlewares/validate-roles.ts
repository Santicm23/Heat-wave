
import { NextFunction, Request, Response } from 'express';


export const isAdminRole = (req: Request, res: Response, next: NextFunction) => {
    
}

export const isSameAccount = (req: Request, res: Response, next: NextFunction) => {
    
}